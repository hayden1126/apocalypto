# STATUS: Apocalypto -- offline apocalypse navigation app

> Living state. Update at the end of every working block so a fresh session can resume from here after `/clear`.

Last updated: 2026-07-10
Branch / worktree: **`origin/main` is at `5e87337` (this handoff sync, PR #10); working tree clean.** Recent merges (2026-07-10): **PR #8** spec Android-first amendment (merge `9471c2e`, underlying `ff55f9f`), **PR #9** P30 cross-device sensor-frame adapter (merge `b4abb3d`, underlying `588489e`/`b8d76ea`), and **PR #10** this post-merge handoff doc-sync (merge `5e87337`). All three of this session's feature branches (`spec-android-first-amendment`, `p30-cross-device-walk`, `handoff-docs-2026-07-10`) are merged and DELETED (local + remote). Earlier: PR #7 slice-0b handoff reconcile (merge `c1ecb7e`, 2026-07-09); slice-0b foundations PR #5/#6 (merges `184c0e0`/`f22a3d2`, 2026-07-09); slice-0 PR #4 (`cc1d867`); the 2 km walk + B1 + earlier doc reconciliations PR #1-#3 (`5b663ff`/`99baadf`/`5d6933e`); spec `docs/superpowers/specs/2026-07-08-mvp-architecture-scope-design.md`. This session's two feature branches (`spec-android-first-amendment`, `p30-cross-device-walk`) are merged but NOT deleted (local + remote); the handoff doc-sync is on branch `handoff-docs-2026-07-10`. Public repo: https://github.com/hayden1126/apocalypto
**HEADLINE (2026-07-08): the FULL ~2 km walk landed and the core thesis PASSES.** Phone PDR + trusted GNSS re-anchoring at ~15-20 s cadence holds street level (held-out radial P95 6.7 m at 15 s) over a real ~2 km out-and-back, and the verdict is robust to a contaminated calibration leg. Raw PDR drift is ~25% (heading-dominated, out-and-back compounds it), so re-anchoring is what rescues it. B1 map-heading (single-pass) still fails, now because the drifted track can't be matched (not the venue). See "Findings (step 2 FULL ~2 km walk)" below. The multi-km blocker is RESOLVED.
Session commits: step-2 work (phone-walk harness + gated-magnetometer experiment, commits `ca664c8`+`a7c6be4`) is **merged to `main` (fast-forward) and pushed to `origin/main`** (2026-07-08); working branch `step2-phone-mag-gate` deleted. This session advanced `origin/main` from `e62fbff` to HEAD: step-1 commits `9ea790d`/`21c5a58`/`f9da938` + step-2 code `ca664c8`/`a7c6be4`, then doc commits (merge sync, shakedown finding, handoff drift fixes, and a GPS-correction pass + brief re-publish that overturned the wrong "14 m GPS ceiling" read: converged GPS was actually ~1-2 m), pushed to `origin/main` (2026-07-08) up to `a28fc64`. The navigation-mitigations research + `PLAN.md` are on `origin/main` (up to `700805e`). **This session (2026-07-08): Workstream A shipped and MERGED to `main`** via PR #1 (merge commit `5b663ff`; branch `workstream-a-trusted-fix-checkpoints`, 4 commits `bef1d62` PLAN fold, `738962f` A1 checkpoint scoring, `f0bd3c9` A2 trusted-fix mask + A3 regression, `02f0c9e` STATUS/PLAN). **Then Workstream B1** on branch `workstream-b1-map-heading` (2 commits `0a73348` map-heading corrector + experiment, `7e2882c` B1 finding docs), **since MERGED to `main` via PR #2 together with the 2 km walk (2026-07-08)**. Session commit range `700805e..HEAD` (see `git log`); a later commit also folded elevation + loop-size walk guidance into `PHONE_WALK_PROTOCOL.md` (pre-walk Q&A: gentle hills OK, avoid stairs, prefer a >= ~1 km distinct-street loop with few laps). Raw phone data is gitignored under `prototypes/pdr-benchmark/data/phone/`.

## Done
- **Git initialized and published.** Public GitHub repo `hayden1126/apocalypto`; initial commit `c2aeedd` (5 files, research phase). Commits use `haydenleung1126@gmail.com` (already public across Hayden's other repos, so no noreply switch).
- **Research/concept phase complete.** Three adversarial multi-agent research runs (105 agents, 56 load-bearing claims verified, ~4.2M tokens, all 2026-07-07). Full sourced reports in `docs/research/`:
  - `2026-07-07-offline-apocalypse-nav-research.md`: core 4 tracks (GPS-denied positioning, CV localization, mesh+trust, hardware/power/EMP).
  - `2026-07-07-capture-features-exploration.md`: Feature 1 (re-survey/mapping) + Feature 3 (fixed-vantage change detection).
  - `2026-07-07-wilderness-drone-exploration.md`: coast/island graceful-degradation go/no-go + optional drone gear tier.
- **Scannable HTML decision brief** published as a Claude artifact: https://claude.ai/code/artifact/fd2b9daf-8ae3-4b4c-8910-cd6b14551dcf (durable source saved at `docs/brief.html`; sections 01 tracks, 02 positioning stack, 03 MVP, 04 capture layer, 05 wilderness/drone reach, 06 risks, 07 method).
- **Project memory** written: `~/.claude/projects/-home-hayden-apocalypto/memory/apocalypto-nav-project.md` (spine + rulings).
- **Research plan** archived at `~/.claude/plans/goofy-wandering-treehouse.md`.
- **PDR + map-matching benchmark + GNSS re-anchoring (step 1): built, measured, committed** (`e62fbff..21c5a58`; `9ea790d` benchmark, `21c5a58` re-anchor). Harness at `prototypes/pdr-benchmark/` (Python): classical PDR (step detection, Weinberg step length, AHRS heading) + GNSS-anchored OSM map-matching (leuven HMM) + periodic GNSS re-anchor, run against the open GEOLOC/ULISS dataset (6 tracks, 4 clean-outdoor and benchmarked, 231-565 m; handheld Xsens MTi-7 IMU held flat/z-up (NOT a phone); foot-mounted + per-stride ground truth). De-risks the pipeline and gives a first signal; not proof (handheld low-cost IMU, sub-km, few-metre GT). Findings below. Plan file: `~/.claude/plans/continue-soft-stream.md`.
- **Step 2 software built (2026-07-08): phone-walk adapter + experiment harness (merged to `main` and pushed, 2026-07-08).** Lets a self-collected phone walk plug into the step-1 harness: `pdr_bench/io/phone.py` (Sensor Logger export → `ImuSession`; reconstructs raw accel = Accelerometer + Gravity, integer-ns timestamp rebasing, phone GPS as the reference `gt_ne`, trivial `phone_georef`); `pdr_bench/eval/phone_metrics.py` (loop closure, held-out re-anchor RMSE, checkpoint errors: the non-circular scores); `pdr_bench/run_phone.py` and `scripts/reanchor_phone.py` (CLIs); empty-strides guard in `pdr/pipeline.py`; `pyubx2` added to `requirements.txt`. 11 new tests, all green; smoke-verified end-to-end on a synthetic walk. Collection protocol (durable): `prototypes/pdr-benchmark/PHONE_WALK_PROTOCOL.md`.
- **Gated-magnetometer experiment built + run (2026-07-08, merged to `main` and pushed).** Tests whether a magnetic-disturbance gate rescues the magnetometer for the fully-GPS-denied heading case. `pdr_bench/pdr/mag_gate.py` (magnitude + dip gate), gated MARG in `pdr/heading.py` (per-sample `updateMARG`/`updateIMU` switch, threaded through `run_pdr` + `heading_only_reference` via `mag_gate_tol`), `scripts/mag_gate_experiment.py`, 6 new tests (28 total green), `out/mag_gate.png`. Verdict in Findings.
- **Deep-research pass on navigation mitigations + a merged next-steps plan (2026-07-08).** A 60-agent adversarially-verified workflow (120 real sources; 3 clusters: heading drift / trusted anchoring / PDR quality) scored every approach against our own runs. Report: `docs/research/2026-07-08-navigation-mitigations-research.md`. Central findings: heading is the root and only the MAP bounds it offline, so **invert the spine** (map = heading corrector, GNSS = scarce along-track hint that must pass a consistency gate); on spoofing, **degrade to dead reckoning, do not try to detect** a competent spoofer; pose is the sleeper risk (our 6.5% is one favorable hold). Also reviewed another session's keyboard plan (`~/.claude/plans/for-these-next-steps-staged-liskov.md`) and merged both into **`PLAN.md`** (the sequenced roadmap, findings per step). Key review correction now in PLAN: `reanchor_phone.py`'s `TRUSTED_ACC_M=8.0` gates on iOS's ~7x-pessimistic reported accuracy, starving the loop; fix it with a self-estimated dispersion quality.
- **Workstream A shipped + MERGED to `main` (2026-07-08, PR #1, merge `5b663ff`): checkpoint scoring + trusted-fix mask + regression harness.** The no-regret keyboard track from `PLAN.md` section 3, built tests-first (44 green). **A1:** `load_checkpoints` (`io/phone.py`) reads Sensor Logger event marks, rebases on the exposed `meta["rebase_t0_ns"]`, converts surveyed `label,lat,lon`/`label,n,e` to the session NE frame via `GeoRef.lonlat_to_ne`, and wires the previously test-only `checkpoint_errors` into a `--checkpoints` flag on `run_phone` + `reanchor_phone` (GPS-free M4 metric); 7 tests. **A2:** new `pdr/trusted_fix.py` `trusted_fix_mask` replaces the `acc<8` gate as two mechanisms + a loose backstop (position-domain max-walking-speed jump gate, dispersion cold-start lock, `acc<50` backstop; IMU-vs-GNSS innovation gate opt-in and off); 7 tests. **A3:** dataset-gated `tests/test_phone_regression.py` locks the shakedown numbers. **Result:** the `acc<8` gate was actively STARVING the loop (8/524 fixes trusted, all in a 7 s window before the first step, so every anchor clamped to one stale pose); the mask trusts 515/524 and moves held-out re-anchor RMSE 30 s 22.2 -> 14.9 m, 15 s 11.4 -> 4.0 m (now matching the report's all-fixes analysis). Blast radius verified contained: `run_phone` metrics byte-identical before/after, loop closure steady 39.0 m, only `heldout_reanchor_rmse` moved. **Honest limit:** catches gross faults only; a slow multipath bias or slow spoof (< ~0.3-0.5 m/s) passes by construction (the degrade-to-dead-reckoning case the cone owns, PLAN.md B3), so this is not a spoof detector.
- **Workstream B1 built + run (2026-07-08, branch `workstream-b1-map-heading`, MERGED to `main` via PR #2): map-derived heading corrector, PRELIMINARY NEGATIVE on the 595 m walk (the decisive 2 km re-run is in Findings below and confirms it, with the confound flipped).** Built the machinery (matched-edge bearing extraction in `mapmatch/match.py`; `map_reanchored_track` in `pdr/reanchor.py` via a behavior-preserving refactor of `reanchored_track`, guarded byte-identical by the A2/A3 regression; `scripts/map_heading_experiment.py`; 4 tests, 48 total green) and measured it on `ma_ling_walk`. **Map-heading correction does NOT bound drift on this walk:** naive ~2.5x worse than pure gyro (loop closure 39 -> 99 m, cross-track 45 -> 99 m); confidence-gated (<8 m snap) trades a cross-track gain (45 -> 38 m) for a worse loop closure (39 -> 136 m); neither near the GNSS-15 s reference (3.5 m). Root cause: 18 m median matched-edge snap distance = wrong-edge snapping of the drifted track in a cramped 23-node / ~50 m-edge / 3-loop block (`out/map_heading.png`). Consistent with pushback #2 (the map can't rescue heading here) but confounded by (a) cramped venue and (b) single-pass matching on drifted input (circular). **NOT a clean KILL** for the inversion; the decisive test is this same machinery on the 2-3 km walk. Design lessons for a viable map-heading corrector: distinct-street geometry, iterated match<->correct, and match-confidence gating.
- **Full 2 km walk processed + harness extended (2026-07-08, branch `walk-2km-passkill`, 51 tests green).** New `scripts/calibrate_leg.py` (derives honest Weinberg k from the first walking run terminated by the deliberate stop; reuses `calibrate_k`/`weinberg_base`; caught the contaminated-leg over-estimate). Held-out **P95** added to the verdict path: `heldout_reanchor_stats(...)->{rmse,p95,xtrack_p95}` in `eval/phone_metrics.py` (the gate is P95-stated), printed by `reanchor_phone.py`, locked in `tests/test_phone_regression.py` (shakedown 15 s P95 7.1 m, 30 s 25.6 m). `map_heading_experiment.py` parametrized (`--k`, `--name`, per-name OSM cache `data/osm_cache/<name>.graphml` -> kills the stale-graph footgun; generic data-driven verdict). Fixed a latent `run_phone.py` crash: the map-matcher drops observations when coverage is thin (here 75/719), so `matched_vs_gps` is now guarded to skip rather than broadcast-error. 3 new `calibrate_leg` unit tests. Verdict + B1 in Findings.

## In flight
- **The roadmap is now `PLAN.md`** (2026-07-08): a sequenced merge of the deep research and the other session's keyboard plan, findings attached per step. Two tracks: (a) **no-regret keyboard work that does NOT wait for the walk** (Workstream A: checkpoint wiring; a new `trusted_fix_mask` seam doing outlier reject + cold-start trim + the accuracy-gate fix; a real-phone regression test); and (b) **research-informed builds** (Workstream B: map-matched heading prototyped on our own 595 m walk, the map-conflict monitor, adaptive trust-driven cadence, pose robustness). **Workstream A shipped (PR #1 merged to `main`); Workstream B1 built + run (branch `workstream-b1-map-heading`, merged to `main` via PR #2) with a PRELIMINARY NEGATIVE (see Done):** map-heading does not bound drift on the cramped 595 m block, but that is venue-confounded, not a clean KILL. **DONE (2026-07-08): the ~2 km walk landed -> core thesis PASSES (see Findings); B1 single-pass map-heading fails again, now because the ~25%-drifted track can't be matched at all (frac_matched 0.10), not the venue -- still not a clean KILL of map-as-heading-bound, since the mechanism works where matching succeeds.** **Chosen parallel keyboard track (2026-07-08/09): pivot to MVP app design -- now ADVANCED to a built slice-0.** The MVP architecture spec is written+approved and the first implementation increment (slice-0: `core/` Rust workspace + `apoc-geo::interp_ne` + the L0 gate `apoc-positioning::gnss_gate::trusted_fix_mask` + a reusable Python-to-Rust oracle harness; 14 tests green, clippy clean) is built and MERGED as PR #4 (`cc1d867`); slice 0b foundations are now built on top (PR #5/#6, open). See "Notes for next session" for the next increment. Lower-priority alternatives still available: a B1 refinement (iterated match<->correct + confidence gating) or B4 pose-robustness prep.
- **Step 2 chosen (option a): self-collected phone walk. Two 30 s indoor test exports + one ~595 m outdoor shakedown collected and analyzed (2026-07-08); findings below.** The Sensor Logger adapter + full pipeline are validated on real iPhone 14 Pro data, and the phone-grade-IMU-noise unknown is answered (drift ~6.5%, ~2x the Xsens, but bounded by re-anchoring). **DONE (2026-07-08): the full ~2 km PASS/KILL walk is collected and analyzed -> PASS at ~15-20 s cadence (see Findings).** It was collected per `prototypes/pdr-benchmark/PHONE_WALK_PROTOCOL.md`, with shakedown-refined setup: (1) warm up GPS to a FULL lock before recording (stand in open sky until fixes stabilize, ~1-3 min; the shakedown's opening static caught the receiver still cold, 4 scattered fixes / 35 m spread, and one 808 m outlier persisted, so re-anchoring also needs outlier rejection); (2) verify dense OSM footway coverage before walking (shakedown map-matching recovered only 44% of edges) -- the shakedown venue's GPS itself was fine (~1-2 m converged), so no better venue is needed for GPS, only for OSM coverage; (3) plan for a ~15-20 s trusted-fix re-anchor cadence, not 30 s (driven by phone heading drift, not GPS); (4) add the protocol's surveyed checkpoints + CW/CCW repeats (shakedown had neither); (5) pass the new walk's own calibration-leg k via `--k` (shakedown gave k=0.537 as a reference). Then run `python -m pdr_bench.run_phone <dir> --k <k>` + `scripts/reanchor_phone.py <dir> --k <k>`, record PASS/KILL, and re-publish the corrected brief. Field-brief artifact: https://claude.ai/code/artifact/21f3dd06-2216-41c0-be46-2a984b1943d6 (source only in session scratchpad). Fixture decision (2026-07-08): the raw shakedown export is NOT committed (privacy); it lives in gitignored `prototypes/pdr-benchmark/data/phone/` (`ma_ling_walk/` + both zips), durable across sessions but out of git. Open question: save the field-brief HTML into the repo or leave it hosted-only. Both previously-deferred items are now DONE via Workstream A (PR #1): the dataset-gated integration test reading the gitignored phone data (A3, `tests/test_phone_regression.py`) and checkpoint-CSV CLI wiring (A1, `--checkpoints`).

## Findings (step 1, 2026-07-07)
- **PDR half PASSES the gate.** Gyro-only PDR (with static-phase gyro-bias removal) holds rmse ~3% of distance on every outdoor track: TEST_02 6.7 m / 231 m, TEST_04 13.2 m / 375 m, TEST_05 18.0 m / 565 m, TEST_06 14.0 m / 465 m. Squarely in the 2-5% regime the literature predicts. Phone-only PDR is viable at street scale for hundreds of metres.
- **The magnetometer HURT.** Naive Madgwick MARG fusion degraded heading badly (rmse 28-137 m) across all tracks; gyro + static-bias-removal wins. Naive mag fusion is not a usable absolute-heading reference; it needs hard gating or a different mechanism.
- **Heading drift is UNBOUNDED without an absolute reference,** so the longest track is the worst. This is the real limiter for the multi-km claim: over km, heading error will exceed street spacing.
- **Map-matching corrects "which street", not "where along it".** Graph/matcher floor is ~5 m (OSM footway-centerline offset from the actual walked path, the pedestrian-graph pitfall quantified). On dense urban (TEST_05) map-matching recovered ~90% of the correct route and cut cross-track error 10.3 -> 7.2 m, but overall rmse only 18 -> 16.5 m because along-track (step-length) error dominates and matching cannot fix it. On open/parking (TEST_06) it was near-neutral. It is NOT a drift-correction oracle: it only disambiguates while drift stays below street spacing.
- **Periodic GNSS re-anchoring BOUNDS the drift, and the required frequency is quantified.** Simulated on the GEOLOC GNSS (`scripts/reanchor_experiment.py`, `out/reanchor_curve.png`): PDR fills the gaps between occasional trusted fixes; a fix every ~30 s (~35 m at walking pace) holds residual RMSE <=5-6 m on every track including urban; every ~15 s reaches ~3-4 m (the raw GNSS noise floor). So the "degraded GPS" premise is workable: continuous GPS is not required, only occasional trusted fixes. Two caveats: a single early anchor is useless (value is in REPEATED anchoring); and each fix must be TRUSTED, so GNSS spoofing (the brief's structural risk) attacks this loop directly.

## Findings (step 2 phone shakedown walk, 2026-07-08)
First real phone walk: iPhone 14 Pro, Sensor Logger 1.60.1 at 100 Hz, ~595 m (3 loops of a ~50 m block, Ma Ling Path, HK), 683 steps, ~8.7 min with 60 s static bookends, a counted 15.3 m / 18-step calibration leg (hydrant -> first lamppost, distance from Google satellite), closed on a fire hydrant. Preceded by two 30 s test clips (indoor) that verified the adapter ingests real iPhone data (columns map by name despite `z,y,x` order; 100 Hz; Accelerometer + Gravity reconstruction gives |a| ~ 9.8) and that GPS is dead indoors by physics. Raw exports are session-scratchpad only (ephemeral, uncommitted: privacy). Verification via `run_phone --k 0.537` + the reanchor metric.
- **Phone PDR drift ~6.5% over ~595 m** (loop closure, GPS-free, k-invariant): 38.6 m end-vs-start on a truly closed loop. ~2x the step-1 Xsens 3%, same order. Loops precess (heading drift), visible as a fanning spiral, not a bias offset.
- **Gyro bias is stable, NOT the culprit.** Opening-vs-closing static yaw-bias drift 0.042 deg/s (~8 deg accumulated heading error over the ~400 s walk = 0.042 deg/s x 403 s / 2, the /2 for a linear ramp after the pipeline removes the opening bias). The 6.5% is gyro random-walk + integration noise, not a fixable single bias. Phone gyro is honestly decent.
- **Honest step length k = 0.537** (median step 0.85 m, a touch long: deliberate pacing) from the counted 15.3 m leg. GPS-calibrated k (0.679) was inflated 1.27x by GPS jitter + an 808 m outlier fix (GPS path 753 m vs true ~595 m). Concrete proof of the protocol rule: never GPS-calibrate k for the headline.
- **Re-anchoring bounds the drift but the phone needs a TIGHTER cadence than the Xsens.** Held-out RMSE at honest k: 15 s -> 4.0 m, 20 s -> 7.1 m, 30 s -> 14.7 m, 45 s -> 24.5 m, 60 s -> 48.7 m. Step-1 Xsens held 5-6 m at 30 s; the phone needs ~15-20 s (a trusted fix every ~20-25 m) to hold street level, because phone HEADING drifts 2x faster (re-anchoring position every 30 s does not fix heading between anchors, and over a ~20 m half-gap a precessing heading throws ~15 m cross-track). This is a PDR-drift penalty, NOT a GPS-quality one (converged GPS was ~1-2 m; see below). The thesis (PDR + periodic trusted re-anchoring) holds on a real phone, with this quantified cadence penalty.
- **GPS is NOT the ceiling (corrected 2026-07-08 after Hayden noted the loop was open-sky, 2-3 storey residential).** iOS self-reported 14.2 m median accuracy, but that is pessimistic: measured directly from the closing 60 s static (55 fixes, phone stationary at the hydrant), ACTUAL precision was std 1.2 m / p95 1.9 m while iOS still reported 8.9 m. Path inflation (753 m GPS vs true 595 m, 1.27x) independently implies ~5 m walking jitter. So converged phone GPS here is ~1-2 m stationary / ~2-5 m walking, well inside the 20-30 m PASS headroom (the "~5 m floor" guard holds). The real GPS issues were (1) COLD START: the opening static caught only 4 scattered fixes (35 m spread) before the receiver locked, so GPS must be warmed to a full lock BEFORE recording; and (2) one 808 m outlier fix, so trusted-fix re-anchoring REQUIRES outlier rejection (also exactly where GNSS spoofing, the brief's structural risk, attacks). The venue was fine; no better venue needed.
- **Map-matching recovered only 44% of edges** (route_edge_overlap 0.444): thin OSM footway coverage on the block and/or drift too large. Check OSM coverage of any multi-km route before walking it.
- **Limits:** n=1 walker/phone, one pose (P1 held-flat), ~595 m (NOT multi-km), GPS not warmed to a full lock at recording start (cold-start garbage in the first ~90 s), single direction (no CW/CCW bracketing), no surveyed checkpoints. First real signal that the phone pipeline works and drift is bounded; not proof. The multi-km hold and the PASS/KILL verdict are still open.

## Findings (step 2 FULL ~2 km walk, 2026-07-08): the PASS/KILL verdict
The blocker walk (`PLAN.md` §2). iPhone 14 Pro, Sensor Logger 1.60.1 @ 100 Hz, ~2288 m GPS path (true ~1.9-2.0 km), ~1682 s walking with 75 s / 65 s static bookends, **out-and-back** (~1 km each way, U-turn at the far end, back to start), single pose P1, no in-app marks. Raw export gitignored at `data/phone/ma_ling_2km/`. Processed on branch `walk-2km-passkill` (off `workstream-b1-map-heading`). Verify: `run_phone`/`reanchor_phone`/`calibrate_leg`/`map_heading_experiment` with `--k`.
- **CORE THESIS: PASS, and k-robust.** Phone PDR + trusted GNSS re-anchoring at the mandated **~15-20 s cadence holds street level over a real ~2 km walk.** Held-out re-anchor (M2/M3), radial P95 / cross-track P95 at k=0.537: **15 s -> 6.7 / 5.5 m; 20 s -> 12.8 / 8.9 m; 30 s -> 24.8 / 15.8 m.** Across the whole honest k-band (0.41-0.60) the 15 s P95 stays 5.5-6.7 m and holds even at the contaminated leg k=0.744 (15 s P95 10.7 m). The PASS gate (radial P95 <= 20-30 m AND cross-track P95 < 10-15 m at cadence <= mandated) is met at <= 20 s. **At 15 s the residual sits at the ~5 m GPS floor** -> PDR + 15 s re-anchoring tracks about as well as the GPS itself. 30 s is marginal (P95 ~25 m = street width), confirming the shakedown's "phone needs ~15-20 s, not 30 s" now at 2 km.
- **Calibration leg is CONTAMINATED; k came from the band, not the leg.** The 13-footfall / 15.9 m leg was walked at 1.223 m/step (deliberate long strides) but the natural 2 km gait averaged ~0.72 m/step, and the Weinberg acceleration base was identical for both (leg 1.645 vs bulk 1.703), so `calibrate_leg` gave **k=0.744, which over-estimates total distance ~1.8x** (3632 m vs true ~2 km). Weinberg assumes stride scales with the accel bounce; exaggerating stride without a matching bounce breaks it. Honest k is ~0.41-0.54 (the same walker+phone's validated shakedown k was 0.537). **The held-out verdict is insensitive to this** because re-anchoring resets position every interval, so k error cannot accumulate past one gap. **LESSON: walk the calibration leg at natural pace, not exaggerated strides.**
- **Raw PDR drift is ~19-25% over 2 km, heading-dominated** (loop closure 430-565 m; `pdr_vs_gps` final_drift 25.4% agrees via a separate code path; pure PDR is 250-290 m off at the ~1 km U-turn). Far worse than the shakedown's 5.2%, because (a) the walk is longer (heading random-walk grows with time) and (b) the out-and-back geometry **compounds** heading error rather than partially closing it like a repeated loop. Re-anchoring rescues 565 m -> 6.7 m. This makes vivid that heading is the root limiter and re-anchoring is essential, not optional.
- **GPS**: 1816/1826 fixes trusted (the A2 mask auto-rejected 10: mild cold-start + a few outliers). Cold start milder than the shakedown (position converged by t~5.5 s), but reported `horizontalAccuracy` is unreliable (pinned 14.25 m for the first ~450 s and last ~350 s, ~2-3 m only mid-walk). Confirms the A2 design: gate on PDR-vs-GPS innovation, never on reported accuracy.
- **B1 (map-heading): single-pass corrector FAILS on the 2 km walk, but NOT for the 595 m walk's reason.** frac_matched 0.10, **70 m median matched-edge snap**, map-heading makes cross-track worse (177 -> 467 m); the GNSS-15 s reference is fine (xtrack 3.5 m). Root cause here is not a cramped venue but that the **~25%-drifted raw track snaps to wrong edges globally** (circular: you need good heading to match, but you match to fix heading). The coverage split proves the **mechanism works where matching succeeds** (well-fit steps: pure xtrack 67.6 -> map 4.6 m) and hurts where it fails. So this is **NOT a clean KILL of map-as-heading-bound**; it is a KILL of *single-pass matching on raw drifted PDR*. Viable path: iterate match<->correct, or map-match a GNSS-re-anchored (~6 m) track. On a GPS-available walk, re-anchoring alone already bounds drift, so the map's heading value is only testable in GNSS-denied stretches (untested here). The OSM graph itself is adequate (110 nodes / 282 edges); drift, not coverage, breaks the match.
- **M4-lite endpoint check inconclusive**: the surveyed far-end coord (22.40589, 114.20764, map-read) sits ~108 m from the GPS track at the U-turn, so it is too imprecise to corroborate; the re-anchored PDR matches GPS (both ~108 m from the coord), i.e. the coord is off, not the PDR.
- **Limits**: n=1 walker/phone, single pose P1, out-and-back single direction (the return retraces, so it is not an independent CW/CCW bracket), contaminated calibration leg (k from the band, not a clean leg), no true surveyed checkpoints. **The multi-km PASS is now demonstrated.** Pose robustness (B4) and the GNSS-denied map-heading value (B1/B2) remain open.

## Findings (gated-magnetometer experiment, 2026-07-08)
Three open-loop heading variants (gyro-only, naive MARG, gated MARG), heading-isolated (GT strides) + open-loop position RMSE, on disturbed tracks TEST_01/03 (indoor office/stairs) and the four clean-outdoor controls; threshold sweep mag_tol {.05,.1,.2} x dip_tol {5,10,20} deg. Heading-isolated RMSE (m):

| track | accept% | gyro | naive | gated(0.10/10deg) | gated(best) |
|---|---|---|---|---|---|
| TEST_01 (disturbed) | 47 | 50.0 | 36.3 | 35.5 | 33.2 |
| TEST_03 (disturbed) | 48 | 2.4 | 35.4 | 16.8 | 6.1 |
| TEST_02 | 51 | 3.7 | 34.1 | 9.8 | 5.1 |
| TEST_04 | 61 | 8.1 | 91.6 | 80.5 | 19.6 |
| TEST_05 | 36 | 17.3 | 104.8 | 14.2 | 8.0 |
| TEST_06 | 34 | 14.8 | 29.5 | 22.4 | 15.5 |

- **Gating always rescues naive MARG** (34-105 m) to far lower error. Confirmed: never fuse mag blind; if mag is used at all, gate it.
- **But it does NOT beat the gyro-only baseline** except where gyro drift is already severe (TEST_01: 50.0 -> 33-35 m; TEST_05 marginal 17.3 -> 14.2 m). Where gyro-only is already good, gating HURTS (TEST_03 2.4 -> 16.8 m, TEST_02 3.7 -> 9.8 m, TEST_04 8.1 -> 80.5 m at a fixed threshold).
- **No universal threshold.** The optimal gate aggressiveness points in OPPOSITE directions by track: TEST_01 (bad gyro drift) wants a loose gate to USE mag; TEST_03 (good gyro) wants a tight gate to REJECT it. You cannot know offline which regime you are in, so there is no deployable fixed setting. Even oracle per-track tuning (gated-best) rarely beats gyro-only on the already-good tracks.
- **Verdict: gating does not make the magnetometer a dependable net-positive over gyro-only for GPS-denied heading.** It fixes naive MARG's catastrophe and helps only once gyro drift is already large, needing unavailable per-environment tuning. The fully-GPS-denied case still needs a foot-mounted ZUPT accessory or accept-drift, not the magnetometer. Strengthens the step-1 mag ruling.
- **Limits:** non-phone Xsens IMU, moderate indoor disturbance (not a rebar-heavy disaster building), heading isolated via GT strides. First signal, not proof.

## Blocked / decisions needed
- **Next-step decision made (2026-07-08): option (a), self-collect one multi-km phone walk.** Tooling built (see Done/In flight). Unknown (1) does it survive PHONE-grade IMU noise? is now ANSWERED by the ~595 m shakedown: yes, drift ~6.5% (2x the Xsens) and bounded by ~15-20 s re-anchoring (Findings above). Unknown (2) does it hold at true multi-km length? is now **ANSWERED: YES, PASS** at ~15-20 s cadence (held-out radial P95 6.7 m at 15 s over ~2 km; see Findings). The methodology breaks the circularity (phone GPS is both reference and re-anchor source) with GPS-free loop closure + held-out GPS cross-validation, and sets explicit PASS/KILL thresholds (~20-30 m over 2-3 km at a mandated re-anchor cadence).
- **The multi-km collection legwork is DONE (2026-07-08): the ~2 km walk landed and PASSES.** The blocker is cleared. Residual open items now: (a) pose robustness (B4) is untested (single pose P1); (b) the GNSS-denied map-heading value (B1 iterated matcher / B2 conflict monitor) is untested (this walk had GPS throughout); (c) a clean calibration leg (walk it at natural pace) for a non-band-derived k on the next collection. The gated-magnetometer experiment (fully-GPS-denied case) and SHL dataset remain un-taken alternatives.

## Notes for next session
- **PICK UP HERE (2026-07-10, latest): the build order PIVOTED to Android-first (both enabling workstreams MERGED); the NEXT increment is the Android Flutter shell (workstream 3), NOT yet started.** Two new devices drove the pivot (a `device-recon` workflow verified the facts, all sourced): a **Huawei P30** (EMUI 12 / Android 10, Google services present, dual-frequency L1+L5 GNSS, a different IMU vendor than the iPhone 14 Pro) is now the daily Android build+test target, and a **2020 Intel MacBook Air** is a periodic iOS verify/deploy station (Xcode 26.3 on macOS Sequoia builds+signs to the iPhone 14 Pro, but a dead-end machine: Tahoe dropped every Intel Air, App Store SDK wall ~April 2027, Flutter Intel-host deprecation announced, thermal throttling). The iPhone 14 Pro stays the validated positioning reference. **The Mac-mini/cloud-CI question is CLOSED for now** (the Air covers iOS; revisit ~early 2027 or when iOS work becomes sustained).
  - **DONE + MERGED -- workstream 1 (spec amendment), PR #8 (merge `9471c2e`, underlying `ff55f9f`):** the spec's "iOS-first" line is amended to Android-first (new spec section 2a + surgical edits to sections 2, 3, 9, 12, 14; architecture unchanged, only build order; the section 2 native-Android dissent is preserved and noted as vindicated on the build-order half).
  - **DONE + MERGED -- workstream 2 code (P30 cross-device loader), PR #9 (merge `b4abb3d`, underlying `588489e`/`b8d76ea`):** the Android frame adapter in `prototypes/pdr-benchmark/pdr_bench/io/phone.py` (`_detect_frame` auto-detects platform from `Metadata.csv`/`TotalAcceleration.csv`; Android accel+gravity are sign-flipped on all 3 axes vs iOS, gyro convention MATCHES, `Orientation.csv` is never read; normalize in code with a TotalAcceleration numeric cross-check, never the in-app "Standardise" toggle -- a read-only A/B on the iPhone `ma_ling_walk` proved a MISSED flip is a silent ~10-20% metric shift NOT a crash) + `session.py`/`run_phone.py` plumbing + the `PHONE_WALK_PROTOCOL.md` Android P30/EMUI section + natural-pace calibration lesson. **8 new tests, 59 green; the iOS path is byte-identical (regression baselines unchanged, real `ma_ling_walk` still 683 steps / k 0.537 / 7.0% drift).** REMAINING for workstream 2 (not blocking WS3): collect the physical P30 walk on the ma-ling routes, write up the iPhone-vs-P30 findings (attacks the n=1-walker/phone caveat -> n=2 phones, still n=1 walker), and add dataset-gated P30 baselines to `tests/test_phone_regression.py`.
  - **NEXT -- workstream 3 (the Android Flutter shell), NOT started.** `flutter create` app at `app/`, `maplibre_gl` (0.26.2) rendering the baked `region-baker/out/ma-ling/1` package FULLY OFFLINE on the P30 (bundle-as-assets then manifest-driven copy to app support dir; rewrite the style's glyphs/sprite/pmtiles refs to `file://`/`pmtiles://file://`; rebake with `bake.py all` if `out/` absent, binaries gitignored), plus the flutter_rust_bridge v2 Dart->Rust->Dart round-trip against `core/apoc-ffi` (new `src/api.rs` calling the `dto` layer only, `capi`'s 4 `apoc_` symbols untouched, pin frb `=2.x.y` across codegen+both deps). This box CAN build Flutter/Android on Linux (only iOS needs the Mac), deploying to the P30 over adb-over-TCP (WSL2-native adb server; `adb tcpip 5555` needs one Windows-side USB attach per phone reboot; flutter ignores `ADB_SERVER_SOCKET`; EMUI needs "Allow ADB debugging in charge only mode"). **BLOCKED on a Phase 0 env bootstrap** (no flutter/dart/adb/Android SDK/NDK/`aarch64-linux-android` rust target/cargo-ndk/frb codegen installed; Java 21 + node present) -- ~5 GB download + PATH setup, **pending Hayden's go-ahead**. Most of WS3 is verifiable WITHOUT the phone (`flutter doctor`, `flutter build apk --debug`, `flutter test` for the style rewrite, `cargo test`/clippy/nm for `api.rs`, the cross-compiled `.so`); only the final on-device `flutter run` (offline map + probe) needs the P30 paired via `adb tcpip` (Hayden). Branch `slice-0c-flutter-shell` off `main`.
  - Full approved plan (all 3 workstreams, detailed): `~/.claude-alt/plans/i-have-access-to-gentle-teapot.md`. This session's feature branches (WS1, WS2, and the PR #10 handoff sync) are all merged and deleted.
- **(superseded 2026-07-10 by the Android-first pivot above): slice 0b foundations are MERGED to `main` (PR #5 apoc-ffi, merge `184c0e0`; PR #6 region-baker, merge `f22a3d2`); the next increment WAS "the Flutter shell", now redefined as the Android Flutter shell above.** Next concrete step: spec->plan->build the Flutter increment: app shell + `maplibre_gl` rendering the baked `region-baker/out/ma-ling/1` package (rebake locally with `bake.py all` if absent; binaries are gitignored), the flutter_rust_bridge Dart round-trip against `apoc-ffi`, and the **Mac-vs-cloud-CI iOS decision** (this WSL2 box cannot build Flutter/iOS; also revisit the spec's "iOS-first" line then). Both slice-0b work branches are DELETED (local + remote, Hayden-approved 2026-07-09) after confirming the one stranded commit (`fca1174`, a docs reconcile) was cherry-picked into the merge-reconcile branch as `ed00c1e`. Group A `slice-0b-apoc-ffi` (branched off `cc1d867`; commit range `cc1d867..slice-0b-apoc-ffi`, counts go stale so `git log` it): `core/apoc-ffi` is the single FFI crossing (spec section 4.1), split `dto` (safe wiring; owns ALL data validation: shapes, finiteness, strictly-increasing time bases, `lock_window >= 1`) + `capi` (C-ABI sensor lane: owned `(ptr,len)` buffers from boxed slices, `catch_unwind` guards, NaN==None only at the C edge, 4 exported `apoc_` symbols, proven from a plain-C `cc` harness in scratchpad); also closes the slice-0 follow-ups (`#![forbid(unsafe_code)]` in `apoc-geo`/`apoc-positioning`, `core/Cargo.lock` tracked); 23 workspace tests green, `clippy --workspace --all-targets -- -D warnings` clean. Group B `slice-0b-region-baker` (branched off `cc1d867`; commit range `cc1d867..slice-0b-region-baker`): `region-baker/` (`bake.py check|clean|tiles|style|assets|manifest|all`, `gen_style.mjs`, `verify_package.py`) baked the first offline package `out/ma-ling/1` (`ma-ling@1`): 2.07 MB PMTiles (36 tiles z0-15; bbox `[114.18764,22.39089,114.22764,22.42089]` confirmed to contain the private 2 km walk extent with 1.3-2 km margin), dark style (71 layers, zero network refs), 4 Noto glyph stacks x 256 ranges (CJK verified), 4 sprites, 1030-file sha256 manifest carrying the Risk-3 contract (style metadata == manifest `package_version`, machine-checked); planet build (`20260708`) and basemaps-assets (commit `028c18f`) both pinned; `verify_package.py out/ma-ling/1` ALL PASS (binaries gitignored; manifest copy committed under `region-baker/manifests/`). **Both branches passed a high-effort adversarial review workflow (2 runs, ~1.2M tokens, 20 confirmed findings) and every finding is fixed + regression-tested.** The two load-bearing catches: (1) `fonts_in` was blind to font stacks inside `case`/`format` expressions, so the package shipped WITHOUT the style-referenced Devanagari stack while the verifier (sharing the blind extractor) said ALL PASS -- now a real expression walker + whole-layer recursion; (2) the FFI boundary validated pointers but not data: a zero-initialized params struct and NaN coordinates panicked the core, and non-monotonic timestamps returned OK with garbage -- all now rejected as invalid args at the `dto` layer. Plan docs committed per branch (`2026-07-09-slice-0b-apoc-ffi.md`, `...-region-baker.md`; the foundations plan + this STATUS ride the ffi branch). **NEXT (both PRs since MERGED): the following increment = Flutter shell rendering the baked package + the frb Dart round-trip + the Mac-vs-cloud-CI iOS decision (and revisit the spec's "iOS-first" line).**
- **(superseded) 2026-07-09: PR #4 is MERGED (`cc1d867`); the next increment is CHOSEN + PLANNED but NOT executed.** Decision: **slice 0b FOUNDATIONS-FIRST** = (A) `core/apoc-ffi` C-ABI crossing over `interp_ne`/`trusted_fix_mask` (flutter_rust_bridge deferred: no Dart/Flutter on this WSL2 box) + (B) `region-baker` + first dark PMTiles package for the Ma Ling bbox, both verifiable on this box, no Flutter/Mac. The Flutter render + the **Mac-vs-cloud-CI iOS decision** are deferred to the following increment (research finding: this box can't build Flutter/iOS, and pros develop cross-platform then CI-ship iOS; a used Mac mini ~$300-600 is the eventual answer for serious iOS). Full approval-pending plan: `docs/superpowers/plans/2026-07-09-slice-0b-foundations-plan.md` (repo copy) and `~/.claude/plans/what-is-the-status-enumerated-shore.md` (machine-local original). Also revisit the spec's "iOS-first" line in light of that finding.
- **(superseded) NEXT SESSION START HERE (2026-07-09): slice-0 of the Rust core is BUILT and open as PR #4; merge it, then pick the next increment.** Both the spec (`docs/superpowers/specs/2026-07-08-mvp-architecture-scope-design.md`, approved) AND the slice-0 plan (`docs/superpowers/plans/2026-07-09-rust-core-oracle-l0-gate.md`, executed) exist -- so the writing-plans step is DONE. The plan covers ONLY a slice-0 increment (workspace + oracle harness + `apoc-geo::interp_ne` + the L0 `trusted_fix_mask` gate), NOT slice 1. Branch `mvp-architecture-spec` is pushed; **PR #4 is OPEN**. **Next concrete step:** (1) review/merge PR #4; (2) choose the next increment, each with its own spec->plan->build -- either **slice 0b** (Flutter + MapLibre-offline + flutter_rust_bridge app shell rendering a dark offline map) or **continue the positioning port toward slice 1** (`pdr`/heading/`reanchor` + the on-device iPhone re-walk). Non-blocking follow-ups recorded on PR #4: innovation-gate oracle fixture (`use_innovation=true`), empty-input + degradation-ladder-rung tests, a CI check that re-dumping reproduces the committed fixtures byte-for-byte, `#![forbid(unsafe_code)]`, and committing `Cargo.lock` when the FFI/binary crate lands. **Stack decided with Hayden:** Flutter (Dart) shell + a shared Rust core (`apoc-*` crate workspace) + thin native capability adapters; **iOS-first** build/test (Hayden has only an iPhone, and the PASS was validated on iPhone); core milestone = slices 0-3 (positioning spine + map + trust backbone + BLE hazard mesh), capture/power/LoRa fast-follow. Spine tension resolved: ship the proven `SpineUnInverted` as the default `FusionPolicy`, keep `SpineInverted` (the FAILED map-heading) designed-in but feature-gated off. Three load-bearing risks flagged in the spec (frame budget vs Ed25519 envelope; heading-source reintroducing the magnetometer; two-reader map-package drift); the port-correctness risk is now retired by the oracle harness. The MVP is still ~8 subsystems (see "MVP definition" below); the spec draws all boundaries but builds behind the slice order, so each subsystem still gets its own spec/plan/build. **Positioning caveats to keep honest downstream: (1) single pose P1 (B4 untested), (2) single walker/phone + single direction, (3) the PASS is for degraded-but-present GPS, not GPS-denied.** The positioning PASS/KILL is RESOLVED -> PASS (full 2 km walk, held-out radial P95 6.7 m at 15 s; see Findings). Walk + B1 merged (PR #2); doc reconciliation merged (PR #3); spec + slice-0 on PR #4 (OPEN). **Env gotcha: cargo 1.96.1 is installed but NOT on the default shell PATH -- prefix cargo commands with `export PATH="$HOME/.cargo/bin:$PATH"`.**
- **Design center is locked: urban/populated disaster + civil unrest.** Phone-first + optional companion hardware (LoRa/solar). Wilderness and drone are gated optional tiers, never core.
- **Key rulings (don't relitigate):** automatic camera self-localization is REJECTED (perceptual aliasing); camera is a mapping tool only. F3 change-detection = near-shippable classical core; F1 re-survey = "dumb" capture/dedup/bulk-sync layer, reconstruction deferred. Coast/island = GO as solo survival mode, NO-GO as a disaster app. Drone = optional, owner-gated, aerial re-survey is the strongest role.
- **MVP definition (buildable, from the brief §03–04):** offline vector maps + routing; positioning L0–L2 (gated GNSS → IMU PDR → OSM map-matching) with an honest uncertainty cone; BLE hazard mesh + optional LoRa; signed + N≥3-corroborated + TTL trust gate; duty-cycled power + dark map; companion LoRa/solar; F3 change-detection core; F1 capture layer; one shared pointer-vs-bulk data model + LocalSend-style transport.
- **Biggest cross-cutting risk beyond PDR:** adversarial: GNSS spoofing is undetectable offline, and no serverless mesh is Sybil-proof. Handled structurally (dead reckoning as trusted backbone; gate every hazard report), not with better sensors.
- To iterate on the brief without rebuilding: edit `docs/brief.html`, re-publish via Artifact (pass the artifact URL above) to the same URL. To re-run/extend a research workflow: scripts are under `~/.claude/projects/-home-hayden-apocalypto/.../workflows/scripts/`.
- **`docs/brief.html` corrected AND re-published (2026-07-08)** to the same artifact URL https://claude.ai/code/artifact/fd2b9daf-8ae3-4b4c-8910-cd6b14551dcf (updates in place, no duplicate). Fixed: three step-1-superseded map-matching claims (Track A card "cuts drift 40-97%" -> "keeps the right street; it does not erase drift"; L2 rung "Drift eliminator" -> "Which-street disambiguator / ~5 m graph-offset floor"; §05 wilderness card "road-graph drift-eliminator" -> "road-graph's which-street correction", the third caught in the handoff drift check); the "Pre-design phase" eyebrow -> "Prototyping -- steps 1-2 measured"; the metabar single-run stats -> 3-run totals (105 agents / 56 claims); the L0 rung cadence 30-60 s -> ~15-30 s; the footer next-step; and a new "Measured since publish" step-1/step-2 callout with the corrected ~1-2 m GPS. Left intentionally: the §07 stat tiles show first-run detail (49 agents / 24 claims) reconciled by the adjacent prose's 3-run total, so not fabricated into all-run numbers.
- **Verification targets:** (Rust core + NEW apoc-ffi, branch `slice-0b-apoc-ffi`) `export PATH="$HOME/.cargo/bin:$PATH"; cd core && cargo test` (23 tests: apoc-geo interp oracle + apoc-positioning 12 unit + 1 oracle + apoc-ffi 9) and `cargo clippy --workspace --all-targets -- -D warnings` (clean); `nm -D target/debug/libapoc_ffi.so | grep ' T apoc_'` (exactly 4 symbols). (region-baker, branch `slice-0b-region-baker`) `python3 region-baker/bake.py check` then `bake.py all` (needs network) then `python3 region-baker/verify_package.py region-baker/out/ma-ling/1` (ALL PASS, offline); pmtiles CLI installs as `~/go/bin/go-pmtiles` via `go install github.com/protomaps/go-pmtiles@latest`. (slice-0 oracle) regenerate oracle fixtures with `cd prototypes/pdr-benchmark && PYTHONPATH=. .venv/bin/python ../../core/oracle/dump_fixtures.py` (should reproduce the committed JSON). (Python prototype) `cd prototypes/pdr-benchmark && .venv/bin/python -m pytest -q` (59 tests: 28 step-1/phone-adapter/metrics/mag-gate + Workstream A checkpoints/trusted_fix/regression + B1 map-heading + 3 calibrate_leg + 8 Android frame-adapter from PR #9) and `.venv/bin/python -m pdr_bench.run --track TEST_05`. For a phone walk: `python -m pdr_bench.run_phone <export_dir>` and `PYTHONPATH=. .venv/bin/python scripts/reanchor_phone.py <export_dir>`. Dataset (DOI 10.57745/ZCBIIB) gitignored under `data/geoloc/`; re-download if absent. Non-obvious bugs guarded: (1) AHRS reports ENU yaw (CCW from East), must be negated to a compass heading or the trajectory mirrors (`tests/test_pipeline.py`); (2) Sensor Logger's Accelerometer excludes gravity, so raw specific force = Accelerometer + Gravity, and its Unix-ns timestamps exceed float64 exact-int range, so rebase in integer ns before converting to seconds (`tests/test_phone.py`).
