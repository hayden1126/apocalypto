[package]
name = "apoc-positioning"
version = "0.1.0"
edition = "2021"

[dependencies]
apoc-geo = { path = "../apoc-geo" }

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
