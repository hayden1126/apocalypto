[package]
name = "apoc-ffi"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib", "cdylib", "staticlib"]

[dependencies]
apoc-geo = { path = "../apoc-geo" }
apoc-positioning = { path = "../apoc-positioning" }
